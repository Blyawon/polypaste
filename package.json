{
  "name": "polypaste",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin – duplicate & translate components with OpenAI, RTL support, and QA checks",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
